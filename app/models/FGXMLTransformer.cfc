component extends="cffractal.models.transformers.AbstractTransformer" singleton hint="Transforms an FG XML input into a struct that maps to MidgetHerpes' PDFs" {

    variables.defaultIncludes = [];
    variables.availableIncludes = [ ];

	function transform( xml,  scopedIncludes, scopedExcludes, allIncludes, allExcludes ) {
        
    }

}